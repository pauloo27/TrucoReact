import Card, {
  cards,
  cardValues,
  getNextCard,
  toTrumpValue
} from "./entities/Card";
import Suit, { suits } from "./entities/Suit";

export { Card, Suit, suits, cards, cardValues, getNextCard, toTrumpValue };
